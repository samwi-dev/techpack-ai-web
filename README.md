# Techpack AI Web

Static deployment repository for Techpack AI.

Upload the production file as `index.html` in the repository root, then enable GitHub Pages from the `main` branch and `/(root)` folder.